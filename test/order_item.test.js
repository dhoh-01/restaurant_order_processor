import { describe, it } from "node:test";
import assert from "node:assert";

describe("order_item tests", () => {
	it('test', () => {
		assert.ok(1);
	})
	it('test2', () => {
		assert.ok(1);
	})
})
describe("order_item tests2", () => {
	it('test', () => {
		assert.ok(1);
	})
	it('test2', () => {
		assert.ok(1);
	})
})
